(if (cl-search "SPACEMACS" my-daemon-name)
    (progn
      ;; (define-key my-mode-map (kbd "M-m M-m h") #'(lambda () (interactive) (sph) ))
      ;; (define-key my-mode-map (kbd "M-m M-m h") #'sph)
      ;; (global-set-key (kbd "M-m M-m s") (kbd "M-m M-m h"))
      ;; (global-set-key (kbd "M-m M-m M-s") (kbd "M-m M-m h"))
      ;; (define-key my-mode-map (kbd "M-m M-m v") #'spv)
      ;; (global-set-key (kbd "M-m M-m S") (kbd "M-m M-m v"))
      ;; (global-set-key (kbd "M-m M-m M-S") (kbd "M-m M-m v"))
      ;; (define-key my-mode-map (kbd "M-m M-m c") #'compile-run)
      ;; (define-key my-mode-map (kbd "M-m h h") 'helm-hoogle)
      ;; (define-key my-mode-map (kbd "M-m h g") 'helm-google)
      (global-set-key (kbd "M-g M-m") 'open-in-org-editor)
      ;; (define-key my-mode-map (kbd "M-m f d") 'dired-jump)
      (define-key my-mode-map (kbd "M-m M-m M-m") 'magit-status)
      ;; (define-key my-mode-map (kbd "M-m M-m _")  'to-underscore) ;; This causes the prefix error
      )
  (progn
    (global-set-key (kbd "M-g M-m") 'open-in-spacemacs)
    ;; (define-key my-mode-map (kbd "M-m M-m") 'magit-status)
    ;; (define-key my-mode-map (kbd "M-m M-h") 'helm-hoogle)
    ;; (define-key my-mode-map (kbd "M-m M-g") 'helm-google)
    ;; (define-key my-mode-map (kbd "M-m h") #'spv)
    ;; (define-key my-mode-map (kbd "M-m s") #'sph)
    ;; (define-key my-mode-map (kbd "M-m s s") #'swiper)
    ;; (define-key my-mode-map (kbd "M-m v") #'spv)
    ;; (define-key my-mode-map (kbd "M-m S") #'spv)
    ;; (define-key my-mode-map (kbd "M-m f d") 'dired-jump)
    ;; (define-key my-mode-map (kbd "M-m M-m _")  'to-underscore) ;; This causes the prefix error
    ))